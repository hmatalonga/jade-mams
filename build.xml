<project name="JadeProject" default="run" basedir=".">
    <presetdef name="javac">
        <javac includeantruntime="false"/>
    </presetdef>
    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist" location="dist"/>

    <target name="init">
        <mkdir dir="${build}"/>
    </target>

    <target name="compile" depends="init" description="compile the source">
        <javac srcdir="${src}" destdir="${build}">
            <classpath location="jade/lib/jade.jar"/>
        </javac>
    </target>

    <target name="clean" description="clean up">
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>

    <target name="run" description="create agents">
        <java fork="true" classpath="jade/lib/jade.jar;build" classname="jade.Boot">
            <arg value="-gui"/>
            <arg value="John:jadeproject.ParticipantAgent(10000);Bob:jadeproject.ParticipantAgent(10000);Jessica:jadeproject.SchedulerAgent(10000)"/>
        </java>
    </target>
</project>
